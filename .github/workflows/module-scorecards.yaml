# Scores modules in the coder namespace against
# .github/scorecard/SCORECARD.md using Claude, then posts (or updates) one
# GitHub Discussion per module plus the pinned "📊 Module Scorecards" index.
#
# The rubric lives in .github/scorecard/SCORECARD.md. The scoring script
# reads it directly at runtime, so editing that file changes the criteria
# for the next run, and pushing a change to it re-runs full scoring.
# Pushes that touch individual modules re-score only those modules; the
# index is rebuilt either way.
#
# Required repository secrets:
#   SCORECARD_ANTHROPIC_API_KEY  Anthropic API key used for scoring
#   SCORECARD_DISCUSSIONS_TOKEN  GitHub PAT with Discussions read/write

name: Module Scorecards

on:
  schedule:
    # Weekly, Mondays 06:00 UTC
    - cron: "0 6 * * 1"
  push:
    branches:
      - main
    paths:
      - ".github/scorecard/SCORECARD.md"
      - "registry/coder/modules/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: module-scorecards
  cancel-in-progress: false

jobs:
  score:
    name: Score modules and update discussions
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: latest

      - name: Determine scope
        id: scope
        env:
          EVENT_NAME: ${{ github.event_name }}
          BEFORE: ${{ github.event.before }}
          AFTER: ${{ github.sha }}
        run: |
          # Full run for schedule, manual dispatch, rubric changes, or when
          # the previous commit is unavailable (force push, new branch).
          if [[ "${EVENT_NAME}" != "push" ]]; then
            echo "args=" >> "${GITHUB_OUTPUT}"
            echo "Full run (${EVENT_NAME})"
            exit 0
          fi
          if [[ "${BEFORE}" == "0000000000000000000000000000000000000000" ]] || ! git cat-file -e "${BEFORE}" 2>/dev/null; then
            echo "args=" >> "${GITHUB_OUTPUT}"
            echo "Full run (no previous commit)"
            exit 0
          fi
          CHANGED=$(git diff --name-only "${BEFORE}" "${AFTER}")
          if grep -q "^\.github/scorecard/SCORECARD\.md$" <<< "${CHANGED}"; then
            echo "args=" >> "${GITHUB_OUTPUT}"
            echo "Full run (rubric changed)"
            exit 0
          fi
          MODULES=$(grep -oP '^registry/coder/modules/\K[^/]+' <<< "${CHANGED}" | sort -u | paste -sd, -)
          if [[ -z "${MODULES}" ]]; then
            echo "args=" >> "${GITHUB_OUTPUT}"
            echo "Full run (no module paths matched)"
          else
            echo "args=--modules ${MODULES}" >> "${GITHUB_OUTPUT}"
            echo "Scoped run: ${MODULES}"
          fi

      - name: Score modules and update module discussions
        env:
          ANTHROPIC_API_KEY: ${{ secrets.SCORECARD_ANTHROPIC_API_KEY }}
          GITHUB_DISCUSSIONS_TOKEN: ${{ secrets.SCORECARD_DISCUSSIONS_TOKEN }}
        run: bun run .github/scorecard/score-modules.ts ${{ steps.scope.outputs.args }}

      - name: Update index discussion
        env:
          GITHUB_DISCUSSIONS_TOKEN: ${{ secrets.SCORECARD_DISCUSSIONS_TOKEN }}
        # The index reads scores back from the module discussions, so a
        # single-module run still produces a complete index.
        run: bun run .github/scorecard/scorecard-index.ts
